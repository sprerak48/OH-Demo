# Optional: Executive Chat conversational AI (OpenAI). Get a key at https://platform.openai.com/api-keys
# Set in .env locally and in Vercel → Project → Settings → Environment Variables for production.
OPENAI_API_KEY=your-openai-api-key-here

# Optional: use a different model or API base (e.g. Azure, proxy)
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1
